{% extends 'base.html.twig' %}

{%  block body %}

    <h1>Review show</h1>
    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Text</th>
            <th>Publicationdate</th>
            <th>Note</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td><a href="{{ path('review_show', { 'id': review.id }) }}">{{ review.id }}</a></td>
                <td>{{ review.text }}</td>
                <td>{% if review.publicationDate %}{{ review.publicationDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ review.note }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('review_index') }}">list</a>
                        </li>
                        <li>
                            <a href="{{ path('review_edit', { 'id': review.id }) }}">edit</a>
                        </li>
                        <li>
                            <a href="{{ path('review_delete', { 'id': review.id }) }}">delete</a>
                        </li>
                    </ul>
                </td>
            </tr>
        </tbody>
    </table>

{% endblock %}